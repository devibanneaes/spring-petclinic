name: java-workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: my-spc
    runs-on: ubuntu-latest
    
    steps:
      - name: Gitcheckout
        uses: actions/checkout@v4

      - name: Build Java with Maven
        run: mvn clean package

      
      - name: Build and Sonar Scan
        run: mvn clean verify sonar:sonar \
          -Dsonar.projectKey=devibanneaes_spring-petclinic \
          -Dsonar.organization=devibanneaes \
          -Dsonar.host.url=https://sonarcloud.io \
          -Dsonar.login=${{ secrets.SONAR_TOKEN }}
       
       
       
       
       
       
       
       
       
       # run: mvn clean verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=devibanneaes_spring-petclinic -Dsonar.organization=devibanneaes -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=${{ secrets.SONAR_TOKEN }}
          
