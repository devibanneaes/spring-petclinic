name: java-workflow

on:
  push:
    branches:
      - main

jobs:
  build:
  - name: my-spc
    runs-on: self-hosted

    steps:
      - name: gitcheckout
        uses: actions/checkout@v4

      - name: buildjavawithmaven
        run: mvn package